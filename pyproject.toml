[project]
name = "trend-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Karan", email = "karanverma55777@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openai>=3.7.0",
    "pydantic>=2.13.5",
    "pytrends>=4.9.2",
    "requests>=2.34.2",
]

[project.scripts]
trend-bot = "trend_bot:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
